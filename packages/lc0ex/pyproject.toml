[project]
name = "lc0ex"
version = "0.1.0"
description = "Linker for Lc0 neural executables"
requires-python = ">=3.13"
dependencies = ["protobuf>=7.35.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lc0ex"]
